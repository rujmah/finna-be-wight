{
  "name": "dual-session-test",
  "description": "Dual apps that use the same session.",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app"
  },
  "dependencies": {
    "express": "3.3.3",
    "connect-redis": "*",
    "jade": "*"
  }
}